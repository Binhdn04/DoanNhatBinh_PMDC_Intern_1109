export type ApplicationStatus = 'SUBMITTED' | 'UNDER_REVIEW' | 'INTERVIEW' | 'ACCEPTED' | 'REJECTED' | 'WITHDRAWN';
export function normalizeSkill(value: string) { return value.normalize('NFKC').trim().replace(/\s+/gu, ' ').toLocaleLowerCase('und'); }
export function calculateMatchScore(studentSkillIds: Iterable<string>, postingSkills: { id: string; name: string; importance: 'REQUIRED' | 'OPTIONAL' }[]) { const owned = new Set(studentSkillIds); const ordered = [...postingSkills].sort((a, b) => normalizeSkill(a.name).localeCompare(normalizeSkill(b.name)) || a.id.localeCompare(b.id)); const total = ordered.reduce((sum, s) => sum + (s.importance === 'REQUIRED' ? 2 : 1), 0); if (!total) return { calculationVersion: 'skill-v1', score: 0, matchedSkills: [], missingSkills: [], reason: 'NO_POSTING_SKILLS' as const }; const matched = ordered.filter((s) => owned.has(s.id)); const weight = matched.reduce((sum, s) => sum + (s.importance === 'REQUIRED' ? 2 : 1), 0); return { calculationVersion: 'skill-v1', score: Math.floor(100 * weight / total + .5), matchedSkills: matched.map((s) => s.name), missingSkills: ordered.filter((s) => !owned.has(s.id)).map((s) => s.name), reason: 'SKILL_OVERLAP' as const }; }
const transitions: Record<ApplicationStatus, ApplicationStatus[]> = { SUBMITTED: ['UNDER_REVIEW', 'REJECTED', 'WITHDRAWN'], UNDER_REVIEW: ['INTERVIEW', 'REJECTED', 'WITHDRAWN'], INTERVIEW: ['ACCEPTED', 'REJECTED', 'WITHDRAWN'], ACCEPTED: [], REJECTED: [], WITHDRAWN: [] };
export const canTransitionApplication = (from: ApplicationStatus, to: ApplicationStatus, actor: 'student' | 'staff') => actor === 'student' ? to === 'WITHDRAWN' && transitions[from].includes(to) : transitions[from].includes(to) && to !== 'WITHDRAWN';
