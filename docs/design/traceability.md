# Acceptance-to-contract traceability

These are required implementation verification scenarios, not executed application tests. They refine RQ-01–RQ-16 and map the documented audit corrections to the [behavior rules](./behavior-rules.md), [OpenAPI](../api/openapi.yaml), UI, authorization, transitions, and retained data. Source paths for modules are in [code structure](../architecture/code-structure.md).

| Audit / requirement and criterion | UI action | API inputs → outputs | Authorization | Transition and persisted effect | Verification scenario |
| --- | --- | --- | --- | --- | --- |
| 1 / RQ-08 acceptance creates one supervised placement | Application Review selects supervisor and dates | listEligibleSupervisors(applicationId) → IDs/names; transitionApplicationStatus(ACCEPTED, supervisorUserId, dates, note) → application/placementId | Own-company Staff/Admin; revalidate eligible supervisor | INTERVIEW → ACCEPTED; command/history, placement, assignment, periods and notification in one transaction | Lookup chosen ID works; missing/ineligible supervisor or reversed dates leaves no partial records; unrelated Staff denied |
| 1 / RQ-01 removed assignment ends future access | Admin Placement Overview changes/revokes supervisor | listSupervisorAssignments; changeSupervisorAssignment(expectedAssignmentId, supervisorUserId/null, reason) → placement | Admin only; Active placement | Revoke row, optionally insert replacement and audit; retain history | Old supervisor denied on detail/files/AI poll immediately; new supervisor allowed; stale expected ID is 409; company cannot reassign |
| 2 / RQ-02 saved skill survives refresh | Profile Skills saves then reloads | replaceMySkills(skills) and getMySkills → same canonical IDs/names/proficiency | Own Student | Atomic replacement of student_skills | Save Java ADVANCED; reload retains it; empty replacement reloads []; normalized duplicate names rejected |
| 3 / RQ-11 private drafts retain attachment selection | Report Editor saves and reloads | createReportDraft/saveReportDraft(period ID, fields, attachment IDs) → WeeklyReport.draft | Owning Student only | Draft fields plus report_draft_documents replace atomically | Refresh preserves attachments; referenced file cannot be removed; Supervisor/Admin never see draft content or links |
| 3 / RQ-11 all submitted versions and feedback remain readable | Report Detail selects version/history | getReport → versions and reviews.reportVersionId | Current placement participant for submitted history | Submission copies immutable content/links; review joins version ID | Two submissions with different files/feedback render correct pairings; later draft stays private; ended placement preserves history |
| 4 / RQ-02/07/11 authorized private documents | Upload and download file controls | createDocumentUpload → API URL/expiry; uploadDocumentContent; completeDocumentUpload; getDocumentDownloadUrl → API URL; downloadDocumentContent | Owner for upload, current linked-record policy for download | PENDING → AVAILABLE/REJECTED; private bytes/metadata | Browser network contacts only proxy/API; invalid digest fails; oversized file 413; expired token 403; revoked supervisor denied with unexpired URL |
| 5 / RQ-11/14 canonical reporting deadline | Reports selects week; Monitoring reads deadline | listReportingPeriods → IDs/week boundaries/dueAt; getMonitoring → deadlines | Placement participants; Admin for monitoring | Acceptance creates periods independently of reports | September 16–22 example yields two exact periods; arbitrary/other-placement ID rejected; private draft does not leak through calendar |
| 5 / RQ-15 deadline notification | Student opens inbox then period/posting | listNotifications → recipient event/target/period ID; markNotificationRead | Own recipient; deep-link access rechecked | Internal scheduler inserts deduplicated due/overdue notification | Two replicas scanning same minute insert once; downtime catch-up creates one overdue event; submitted/ended placements produce none; future windows remain pending |
| 6 / RQ-16 AI uses authorized stable inputs | Match explanation or version-specific summary | createAiJob(postingId or reportVersionId) → source identity/fingerprint; getAiJob → advisory status/result | Own Student match; assigned Supervisor/Admin report | Immutable input_snapshot; PENDING → PROCESSING → SUCCEEDED/FAILED | Change skills after enqueue and confirm old snapshot used; distinct users do not share cache; source authorization revoked before poll is denied |
| 6 / RQ-16 provider/worker failure cannot block core flows | AI unavailable state while original source remains visible | getAiJob → safe error/status | Requester with current source access | Expired claim requeued up to three attempts; fenced result writes | Kill worker after claim, reclaim after 120 seconds; old token cannot finish; third failure terminal; report/application work remains available |
| 7 / RQ-03/05 deterministic score and empty groups | Posting Editor declares skills; Student views/sorts score | publishPosting; getPosting/listPostings → skill-v1 score/reason | Own-company Staff/Admin publish; Student discovery | Canonical normalized skill IDs; no persisted score | Run all score vectors; Java vs Java+SQL yields 67; [] publishes with score 0/NO_POSTING_SKILLS; omitted undeclared skills fails publish |
| 7 / RQ-02/04 preferences affect order only | Discover selects relevance/newest/score | listPostings(filters, sort) → ordered page | Student eligible postings only | Read-only deterministic projection | Outside-preference eligible posting remains present; ties repeat in stable ID order; changing proficiency alone leaves score unchanged |
| 8 / RQ-01 valid active role selects permissions | Account menu switches role | setActiveRole(activeRole) → me/accessToken/expiresAt; getMe | Current role membership and session | Session role/version changes; no business record mutation | Switch Student → Supervisor clears old UI cache; old token is 401; unavailable role is 403 without session change |
| 9 / RQ-07 complete application required | Apply completes cover note and CV | createApplication(required fields) → 201 application/history | Own Student; eligible posting and owned AVAILABLE CV | One application/student/posting plus immutable submission snapshot | Omitted/empty/whitespace cover note 400 with no row; duplicate submission 409; later profile changes preserve snapshot |
| 10 / RQ-08 acceptance retry is safe | Application Review retries after timeout | transitionApplicationStatus(same command) → 200 existing placement | Recheck current Staff/Admin access | Exact replay has zero writes; conflicting replay 409 | Two concurrent identical accepts produce one placement/history/notification/calendar; changed supervisor/date/note is rejected even after reassignment |
| 10 / RQ-11 reviewer acts on viewed version | Review Detail approves or requests revision | reviewReport(reportVersionId, outcome, feedback) → history or 409 | Assigned Supervisor/Admin; Active placement | Locked current-version comparison; one review/version | Review v1 after v2 submission returns 409 without writes; preserve feedback and require reread; missing revision feedback 400 |
| 11 / all route-dependent flows | API-backed forms and adapters | Canonical /api/v1 prefix; POST status; PATCH report draft; 400 invalid feedback | Same policies as source operation | No alternate transition routes | Contract validation resolves all refs/operation IDs; sequence arrows match published method/path/error |
| RQ-03/06 publication and saves | Publish/close/archive, save/remove | publishPosting, transitionPostingLifecycle, savePosting, listSavedPostings | Own-company Staff/Admin; own Student saves | Lifecycle history; unique student/posting save | Closed posting rejects new applications/saves but remains in prior saver's list; repeated save yields one row |
| RQ-09/10 placement/task lifecycle | Supervisor creates task; Student changes status | createTask(required title/description/priority); updateTaskStatus; transitionPlacementLifecycle | Assigned Supervisor/Admin creates/ends; own Student updates | Active-only task writes; status is human-reported | Due today not overdue; unfinished past-due is overdue; completion removes overdue; ended placement rejects writes |
| RQ-12/13 separate assessments | Student and evaluator use separate forms | upsertSelfAssessment; upsertPerformanceEvaluation → independent records | Own Student versus assigned Supervisor/Admin | Separate draft/submitted aggregates; human completion decision | Student cannot write evaluator decision; no combined grade returned; submitted assessment survives placement end |
| RQ-14/15 read-only visibility | Monitoring drill-through; inbox marks read | getMonitoring; listNotifications; markNotificationRead | Admin dashboard; own recipient inbox | Reads don't modify source; read_at only for caller | No monitoring mutation route; another recipient cannot read/mark a notification; inaccessible target reveals no record |

All eleven audit findings have a specified design correction. Implementation sign-off still requires these scenarios to pass against the built system; a documentation mapping alone is not runtime evidence.
