# InternHub Contracts

**Owner:** API and web teams. **Status:** planned; no package dependencies are initialized here.

This package will contain versioned, shared REST DTOs and API contract types generated from or validated against the API's OpenAPI specification.